# OpenAI-compatible rewriter (Groq, OpenRouter, Ollama, or OpenAI).
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
ADH_REWRITER_MODEL=gpt-4o-mini

# Optional later-phase remote detectors (not used by the open-core loop).
PANGRAM_API_KEY=
GPTZERO_API_KEY=
